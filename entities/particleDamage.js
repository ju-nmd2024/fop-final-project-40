export default class DamageParticle {
    constructor(x, y, damageAmount) {
        this.x = x;
        this.y = y;
        this.damageAmount = damageAmount;
    }
    draw() {
        for (let i = 0; i > 5; i++) {
            
        }
    }
}